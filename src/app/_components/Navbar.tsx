import React from 'react'

// to do
const Navbar = (): React.JSX.Element => {
  return (
    <div style={{ padding: 20, width: '100%', background: 'red' }}>navigation</div>
  )
}

export default Navbar
